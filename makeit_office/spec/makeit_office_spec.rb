require "spec_helper"

describe MakeitOffice do
  it "has a version number" do
    expect(MakeitOffice::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
